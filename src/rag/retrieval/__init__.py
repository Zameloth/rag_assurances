"""Question → candidates: condenser · short-circuit · legs · expansion · fusion · rerank · quota (SPEC §8–§9).

`short_circuit.py` (#27) lands first — it decides whether the condenser (§8, still to
come) runs at all. The rest is empty until the retrieval ticket. Build order step 3.
"""
